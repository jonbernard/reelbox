# Database (local dev uses docker-compose.dev.yml on localhost)
DATABASE_URL="postgresql://reelbox:reelbox@localhost:5432/reelbox"

# Path to video files (used by the media API to serve videos)
# For local dev, this is the same as your myfaveTT export path
VIDEO_BASE_PATH="/path/to/myfavett/export"

# Path to myfaveTT export folder (used by import and watch scripts)
MYFAVETT_EXPORT_PATH="/path/to/myfavett/export"
